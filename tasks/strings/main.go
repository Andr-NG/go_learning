package main

import (
	"fmt"
// "go_learning/tasks/strings/replace_spaces"
)


func main() {
	// result := replace_spaces.ReplaceSpaces()
	result := ReverseString("héllo world!")
	// result := VerifyPalindrome("car")
	// result := TrimSpaces(" hello   world   ")
	// result := FindNonRepeatingChar("swiss") // string(result)
	// result := TitleEveryWord("hello world from go")
	// result := CountWords("go is fun and go is fast")
	// result := CheckAnagram("silent", "listen") 
	// result := FindLongestWord("Go makes concurrency concurrence easy")
	// result := HideLastWord("my password is secret123")
	fmt.Println(result)
}